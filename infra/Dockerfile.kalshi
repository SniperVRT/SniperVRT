FROM python:3.11-slim
WORKDIR /app
RUN useradd -m -u 1000 app
COPY pyproject.toml ./
RUN pip install --no-cache-dir -e .
COPY . .
RUN chown -R app:app /app
USER app
EXPOSE 8080
CMD ["python", "-m", "kalshi_engine.worker"]

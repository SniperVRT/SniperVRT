# Full System Audit — 2026-06-12

## Headline

| Metric | Value |
|---|---|
| Tests passing | **205 / 205** (96 Kalshi + 109 copy-trade) |
| Modules | 99 (41 Kalshi + 54 copy-trade + 4 unified) |
| Lines of code | 11,099 total; 9,790 non-test |
| SQLite tables | 35 Kalshi + 21 copy-trade + 3 unified = **59 tables** |
| CLI commands | 14 Kalshi + 21 copy-trade = **35 commands** |
| Schema applies cleanly on fresh DB | Yes |
| All modules import cleanly | Yes (33/33) |
| Stubbed code paths | 1 (Kalshi reconcile-poll, marked safe-by-default) |
| Plaintext secrets in repo | None |
| Days to June 22 deadline | **10** |

## What works end-to-end (verified during this audit)

- `kalshi_engine init-db`, `copy-trade init-db`, unified init — all apply 59 tables cleanly on a fresh path
- Healthcheck HTTP server returns 200 OK with valid JSON body and Prometheus-format metrics
- Promotion gate on a fresh DB correctly returns `ready=False` with 4 distinct blockers (history, paper runs, Spearman correlation, testnet roundtrip)
- All 33 sampled modules import without circular dependencies or missing-init errors
- Kalshi `signed_post` produces valid 256-byte RSA-PSS signatures
- Hyperliquid `vault_usd_transfer` wired through Fernet-encrypted key path
- 24h lockup tracking enforced in both live + paper executors (caught by tests)
- Mode isolation: paper subscriptions never leak into live executor's view (regression test)
- Stress test scenarios + VaR/CVaR pure-function paths verified
- FIFO tax lots produce valid Form 8949 CSV (header + rows)
- Cross-system safety cascade engages locks in BOTH subsystem DBs

## What is intentionally a stub (and why it's safe)

- `kalshi/kalshi_engine/worker.py:_reconcile_job` — logs intent only; does not yet GET `/portfolio/orders/{id}`. **Why safe:** scheduler keeps running even if connector is unreachable; reconciliation will be hooked up once a real live order is placed (Day-6 milestone).

## Issues found in audit

### Uncommitted work
- `copy_trade/external/twitter.py` (twscrape integration, sync wrapper, sentiment scoring)
- `copy_trade/external/onchain.py` (Hyperliquid funding/OI fetch + risk-on score)
- `copy_trade/regime/__init__.py` (empty init for future regime classifier)
- **No tests** for the above three modules yet
- **Not staged or committed** as of audit time

### Deferred from MASTER_PROMPTS that remain not built
| Item | Status | Risk to deadline |
|---|---|---|
| Twitter scraper | Partially built (untracked, no tests) | Low — alpha-enhancement only |
| HMM regime classifier | Not built | Low — needs 90d of data anyway |
| A/B framework | Not built | Medium — useful for tuning post-live |
| Investor PDF report | Not built | None — cosmetic |
| Kalshi WebSocket | Not built | Medium — required for real-time exit on edge decay |
| mypy strict + CI workflow | Not built | None — solo development OK without |

### Documentation gaps
- `docs/ARCHITECTURE.md`, `docs/RUNBOOK.md`, `docs/SETUP.md`, `docs/DECISIONS.md`, `docs/COMPLIANCE.md` from Prompt 5 — **not built**
- Existing `docs/architecture.md` (lowercase) refers to the original TypeScript orchestrator, not the Python subsystems

### Code-quality observations
- No file exceeds 500 LOC — largest is `copy_trade/cli.py` at 454 lines (could be split into command modules but not urgent)
- No mypy run, no ruff lint enforcement
- No coverage measurement
- All connector tests rely on either mocks or pure-function paths — **no integration test ever hits Hyperliquid or Kalshi APIs**

### Schema observations
- Copy-trade `subscriptions` schema includes `entry_equity_usdt` for watchdog, but the live + paper executors do NOT yet persist a value into this column at subscribe time. **Risk:** watchdog returns no decisions because `entry_equity_usdt = NULL` on every row. **Fix needed before live.**
- Kalshi schema has both lowercase `live_orders` (Week 3) and TypeScript orchestrator schema files coexisting — no conflict but worth a future cleanup pass.
- No automated schema-migration mechanism — all schema is applied via `executescript` of a single SQL file. Additive-only edits remain safe; deletions or column-type changes would need a migration framework.

### Operational gaps
- No CRON or systemd unit installed on the host — the artifacts live in `infra/` but nothing runs them yet
- No `data/backups/` directory pre-created (backup script creates it on first run; just noting)
- No `KEYSTORE_PASSPHRASE` documented as a required env var for `worker --live`
- Healthcheck server not yet started inside `worker.py` (only standalone)

### Security review
- Private key: **encrypted on disk via Fernet + PBKDF2 (390k iterations)** — passes audit
- No private key appearing in any committed file (`grep` confirms)
- API keys via env vars or .env file (`.env*` already in `.gitignore`)
- Permissions: backup.sh marked executable, encrypted blobs would be `data/keystore.bin` (not yet generated)
- Notification webhooks store URLs in settings — a webhook URL is a secret in practice but not in security model; acceptable
- HTTP healthcheck binds to `0.0.0.0` — if deployed publicly, **firewall ports 8080/8081**

### Test-suite robustness
- All 205 tests pass on every run (deterministic, no flakes observed)
- Tests cover: math (sharpe/VaR/Calmar/PAV/correlation), state machines (subscriptions, lockup), data quality (validators, circuit breaker), persistence (event log, tax lots), CLI helpers
- **Missing coverage:** worker's APScheduler integration, Hyperliquid SDK round-trip against testnet (would need real credentials), notification webhook send (HTTP-mocked but not real)

## Critical path to live (June 22 deadline = 10 days)

| Priority | Action | Effort | Why |
|---|---|---|---|
| 1 | Commit + test the 3 untracked external modules | 1h | Stop drift between repo and disk |
| 2 | Wire `entry_equity_usdt` into live + paper subscribe | 1h | Watchdog is no-op without it |
| 3 | Run `copy-trade poll-leaderboard` daily NOW | 0h (just cron it) | Snapshots are pre-req for every other gate |
| 4 | Run real Hyperliquid testnet roundtrip with funded test wallet | 2h | Required by promotion gate |
| 5 | Set up CRON for `infra/backup.sh` every 6h | 5min | Recoverability |
| 6 | Run `copy-trade keystore-init` with a fresh wallet | 5min | Required for `--live` |
| 7 | Configure Slack/Discord webhook in settings | 5min | Alerts on first live action |
| 8 | Run `copy-trade promotion-check` and verify all blockers known | 5min | Confidence build |
| 9 | First mainnet `copy-trade rebalance --live` with ≤ $200 | 30min | Real-world validation |
| 10 | Daily review: `copy-trade status` + `risk-snapshot` + `stress-test` | 10min/day | Observability |

## Recommended next single prompt

> **Goal:** Close the audit-identified gaps before any `--live` action.
>
> 1. Stage and commit `copy_trade/external/twitter.py`, `copy_trade/external/onchain.py`, `copy_trade/regime/__init__.py`. Add tests for `score_text` (twitter) and `aggregate_signal` (onchain) using canned input; no live API calls.
> 2. Modify `copy_trade/execution/subscriptions.py:execute_decisions` and `copy_trade/paper/executor.py:execute_paper_decisions` so the subscribe INSERT also writes `entry_equity_usdt`: pull current vault equity from `HyperliquidConnector.vault_details(uid)` at subscribe time, or accept it as a `decision.context` field for testability. Backfill column for existing rows to current `allocated_usdt` as a one-shot SQL.
> 3. Start the healthcheck server inside both `worker.py` modules so `curl http://localhost:8081/health` works once the worker is running.
> 4. Write `docs/ARCHITECTURE.md` (one mermaid flow diagram + table inventory), `docs/RUNBOOK.md` (5 incident scenarios), `docs/SETUP.md` (fresh-host deployment from zero).
> 5. Add `.github/workflows/test.yml` running both pytest suites on push.

That's all. After that step, `copy-trade promotion-check` should pass once 7 days of polling have accumulated and a real testnet roundtrip is run.
